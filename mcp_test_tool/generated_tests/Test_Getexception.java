import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Getexception {
    @Test
    public void testGetexception() {
        // Example: {'name': ''}
        // assertEquals(expected, new ClassName().getException());

        // Example: {'name': 'a'}
        // assertEquals(expected, new ClassName().getException(a));

        // Example: {'name': 'test'}
        // assertEquals(expected, new ClassName().getException(test));

    }
}
