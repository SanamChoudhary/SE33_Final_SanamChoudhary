import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Isexception {
    @Test
    public void testIsexception() {
        // Example: {'name': ''}
        // assertEquals(expected, new ClassName().isException());

        // Example: {'name': 'a'}
        // assertEquals(expected, new ClassName().isException(a));

        // Example: {'name': 'test'}
        // assertEquals(expected, new ClassName().isException(test));

    }
}
