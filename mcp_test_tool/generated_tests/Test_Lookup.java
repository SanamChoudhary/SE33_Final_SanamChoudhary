import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Lookup {
    @Test
    public void testLookup() {
        // Example: {'key': ''}
        // assertEquals(expected, new ClassName().lookup());

        // Example: {'key': 'a'}
        // assertEquals(expected, new ClassName().lookup(a));

        // Example: {'key': 'test'}
        // assertEquals(expected, new ClassName().lookup(test));

    }
}
