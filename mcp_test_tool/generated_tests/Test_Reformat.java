import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Reformat {
    @Test
    public void testReformat() {
        // Example: {'input': ''}
        // assertEquals(expected, new ClassName().reformat());

        // Example: {'input': 'a'}
        // assertEquals(expected, new ClassName().reformat(a));

        // Example: {'input': 'test'}
        // assertEquals(expected, new ClassName().reformat(test));

    }
}
