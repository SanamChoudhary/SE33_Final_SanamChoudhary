import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Endswith {
    @Test
    public void testEndswith() {
        // Example: {'str': ''}
        // assertEquals(expected, new ClassName().endsWith());

        // Example: {'str': 'a'}
        // assertEquals(expected, new ClassName().endsWith(a));

        // Example: {'str': 'test'}
        // assertEquals(expected, new ClassName().endsWith(test));

    }
}
