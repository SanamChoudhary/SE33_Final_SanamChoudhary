import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Subsequence {
    @Test
    public void testSubsequence() {
        // Example: {'startIndex': 0}
        // assertEquals(expected, new ClassName().subSequence(0));

        // Example: {'startIndex': 1}
        // assertEquals(expected, new ClassName().subSequence(1));

        // Example: {'startIndex': -1}
        // assertEquals(expected, new ClassName().subSequence(-1));

        // Example: {'startIndex': 100}
        // assertEquals(expected, new ClassName().subSequence(100));

        // Example: {'startIndex': -100}
        // assertEquals(expected, new ClassName().subSequence(-100));

        // Example: {'endIndex': 0}
        // assertEquals(expected, new ClassName().subSequence(0));

        // Example: {'endIndex': 1}
        // assertEquals(expected, new ClassName().subSequence(1));

        // Example: {'endIndex': -1}
        // assertEquals(expected, new ClassName().subSequence(-1));

        // Example: {'endIndex': 100}
        // assertEquals(expected, new ClassName().subSequence(100));

        // Example: {'endIndex': -100}
        // assertEquals(expected, new ClassName().subSequence(-100));

    }
}
