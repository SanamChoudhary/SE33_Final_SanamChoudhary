import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Indexof {
    @Test
    public void testIndexof() {
        // Example: {'startIndex': 0}
        // assertEquals(expected, new ClassName().indexOf(0));

        // Example: {'startIndex': 1}
        // assertEquals(expected, new ClassName().indexOf(1));

        // Example: {'startIndex': -1}
        // assertEquals(expected, new ClassName().indexOf(-1));

        // Example: {'startIndex': 100}
        // assertEquals(expected, new ClassName().indexOf(100));

        // Example: {'startIndex': -100}
        // assertEquals(expected, new ClassName().indexOf(-100));

    }
}
