import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Replaceall {
    @Test
    public void testReplaceall() {
        // Example: {'replaceStr': ''}
        // assertEquals(expected, new ClassName().replaceAll());

        // Example: {'replaceStr': 'a'}
        // assertEquals(expected, new ClassName().replaceAll(a));

        // Example: {'replaceStr': 'test'}
        // assertEquals(expected, new ClassName().replaceAll(test));

    }
}
